import Button from '@/components/ui/Button';
import { FC } from 'react';

interface pageProps {
  
}
 
const Page: FC<pageProps> = () => {
  return (
    <Button
      isLoading={true}
    >

    </Button>
  );

}
 
export default Page;